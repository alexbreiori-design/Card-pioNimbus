/** Frames do splash de carregamento da landing (mascote + comidas). */

/** Desktop: 3 mascotes (burger → pizza → sorvete). Celular: só o 1º. */
export const LANDING_LOAD_FRAMES = [
  '/images/landing/load/cloud-burger.webp',
  '/images/landing/load/cloud-pizza.webp',
  '/images/landing/load/cloud-icecream.webp',
];

/** Espaçamento entre mascotes no ciclo do splash (desktop). */
export const LANDING_SPLASH_STAGGER_S = 0.35;
